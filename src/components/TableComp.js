import React, { useState, useEffect } from "react";
import Table from "react-bootstrap/Table";

const TableComp = (props) => {
  //   const { localData, onDelete } = props;
  const arr = [...props.todos];
  let propNames = {
    id: "id",
    email: "email",
    first_name: "first_name",
    last_name: "last_name",
  };

  return <div></div>;
};

export default TableComp;
